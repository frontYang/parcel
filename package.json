{
  "name": "parcel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "NODE_ENV=production && parcel build src/*.html -d build --public-url ./",
    "dev": "parcel src/*.html --no-hmr --no-cache --no-source-maps -p 8080 --open"
  },
  "dependencies": {
    "autoprefixer": "^8.2.0",
    "babel-preset-env": "^1.6.1",
    "node-sass": "^4.8.3",
    "postcss-modules": "^1.1.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0"
  }
}
